/**
 * ====================================================================
 * APLIKACE BYLA PŘEPSÁNA DO PYTHONU POMOCÍ KNIHOVNY STREAMLIT
 * ====================================================================
 * 
 * Původní Angular aplikace byla nahrazena novou verzí v Pythonu.
 * 
 * Jak spustit novou aplikaci:
 * 1. Ujistěte se, že máte nainstalovaný Python a Streamlit (`pip install streamlit`).
 * 2. Otevřete terminál v kořenovém adresáři tohoto projektu.
 * 3. Spusťte příkaz:
 * 
 *    streamlit run app.py
 * 
 * Tento soubor (index.tsx) a ostatní soubory pro Angular (.ts, .html) 
 * se již pro běh aplikace nepoužívají.
 * ====================================================================
 */

console.info("Aplikace byla přepsána do Pythonu (Streamlit). Spusťte ji pomocí 'streamlit run app.py'. Tento soubor se již nepoužívá.");

// Původní kód pro bootstrap Angular aplikace je záměrně odstraněn.

// AI Studio always uses an `index.tsx` file for all project types.
